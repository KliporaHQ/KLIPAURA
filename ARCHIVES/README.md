# ARCHIVES

This folder is reserved for **optional legacy snapshots** (exports, one-off backups, or deprecated assets). The active codebase lives at the repo root (`klip-dispatch/`, `klip-avatar/`, etc.). Do not treat files here as part of the default runtime path unless you explicitly restore them.
