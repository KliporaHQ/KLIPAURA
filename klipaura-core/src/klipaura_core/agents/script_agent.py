"""Stub for legacy script_agent import."""

from __future__ import annotations

import logging

logger = logging.getLogger(__name__)


def generate_script_with_llm(*args, **kwargs) -> str:
    logger.warning("generate_script_with_llm() — stub (klipaura_core.agents not active)")
    return ""
